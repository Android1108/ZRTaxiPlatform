<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="80dp"
    android:layout_height="80dp">

    <ImageView
        android:id="@+id/view_photo_image_photo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:transitionName="zoomImage" />

    <ImageView
        android:visibility="gone"
        android:id="@+id/view_photo_image_close"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="right"
        android:src="@drawable/ic_delect" />

    <TextView
        android:id="@+id/view_photo_text_cover"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/orange1_80"
        android:gravity="center"
        android:paddingBottom="@dimen/layout_margin_mini"
        android:paddingTop="@dimen/layout_margin_mini"
        android:text="封面"
        android:textColor="@color/white"
        android:textSize="@dimen/text_size_small"
        android:visibility="gone" />

</FrameLayout>